import { AI_PLATFORM_CONSTANTS } from '../shared/constants';

/** Embedding dimensions for the configured model (text-embedding-3-small). */
export const EMBEDDING_DIMENSIONS = AI_PLATFORM_CONSTANTS.EMBEDDING_DIMENSIONS;
